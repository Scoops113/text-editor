{
  "name": "JATE",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
  "start": "node server/server.js",
  "start:dev": "npx nodemon server/server.js --ignore client",
  "build": "npm run build --prefix client",
  "install": "npm install --prefix client && npm install --prefix server",
  "client": "npm run start --prefix client",
  "server": "./node_modules/.bin/nodemon server/server.js --ignore client"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "if-env": "^1.0.4"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "css-loader": "^6.11.0",
    "nodemon": "^2.0.4",
    "style-loader": "^3.3.4"
  }
}
